#include <iostream>
using namespace std;

int main() {
    // Empty pointer
    // 1.Use to initializate the pointer varible
    //int * p = NULL;

    // 2.Empty pointer are not allowed to visit
    // The memory number between 0 and 225 are used by system so it is not allowed to visit
    //* p = 100;

    return 0;
}